apiVersion: v1
kind: Service

metadata:
  name: blue-green-service
  namespace: prod
spec:
  type: NodePort

  selector:
    app: blue-green-app
    version: green

  ports:
  - port: 80
    targetPort: 3000
